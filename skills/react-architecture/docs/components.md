# Components

Use `components/{feature}/` for reusable UI components.

- Components render views, receive props, and call callbacks.
- Do not put model logic, API calls, or query setup in components.
